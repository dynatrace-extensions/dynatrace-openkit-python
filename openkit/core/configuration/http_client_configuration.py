class HttpClientConfiguration:

    def __init__(self, base_url: str, server_id: int, application_id: str):
        self.base_url = base_url
        self.server_id = server_id
        self.application_id = application_id
